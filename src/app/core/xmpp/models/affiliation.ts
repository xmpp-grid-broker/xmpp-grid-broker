/**
 * The names of possible affiliations as
 * defined in xep-0060.
 */
export enum Affiliation {
  Owner = 'owner',
  Publisher = 'publisher',
  PublishOnly = 'publish-only',
  Member = 'member',
  None = 'none',
  Outcast = 'outcast'
}
